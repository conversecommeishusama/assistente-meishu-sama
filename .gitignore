.env
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.cleanup_quarantine/
restored_from_pyc/
.vscode/

logs/*.log
logs/*.tmp
logs/*.jsonl

*.jks
*.keystore

android-app/
admin-android-app/

experiments/rebuilt_large_indexes/
experiments/uploaded_indexes_backup_*/
experiments/uploaded_indexes/

data/clean_corpus/
data/publication_sources/
data/support_tickets/
exports/

textos_portugues/
textos_japones/

static/downloads/*.apk

*.tmp
*.bak
*.backup
*.zip
logo.before-*.png
logo.original.png

# Material de referência com direitos autorais de terceiros (IMMB) -- nunca
# commitar, só para consulta local durante análise de corpus/glossário.
referencia_manuais/
referencia_zenshu_rokkan_DIREITOS_AUTORAIS_APAGAR_DEPOIS/

# reports/ é convenção do projeto para rascunhos/relatórios de trabalho,
# sempre fora do git (2026-08-03: `git add` explícito demais já commitou
# arquivos daqui por engano 2x na mesma sessão -- isso fecha a causa raiz).
reports/

# Dashboard da setorização de Diadema (2026-08-12) -- contém dados pessoais
# reais (nome, telefone, endereço de ~806 pessoas); só existe no servidor,
# nunca no versionamento. Rota protegida: /diadema.
goshinsho/data/dashboard_diadema/


# 2026-08-16: efetivar decisão do usuário -- glossario_traducao.json e
# livros_publicacao_pt_revisado/ ficam FORA do git (edição ativa; não commitar).
glossario_traducao.json
livros_publicacao_pt_revisado/

# Backups de edição espalhados (terminam em .bak_<timestamp>Z) -- nunca versionar.
*.bak_*

# Índices FAISS/pkl legados na raiz (o app usa experiments/uploaded_indexes/).
/*.pkl
/*.faiss
