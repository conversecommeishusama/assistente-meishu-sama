flask
python-dotenv
supabase
openai
stripe
sentence-transformers
faiss-cpu
rank-bm25
torch
numpy
requests
boto3
sentry-sdk[flask]
# 2026-09-01: Leitura Colaborativa — áudio edge-tts (MP3) e acesso direto ao
# Postgres (progresso de leitura + colaborações).
edge-tts
psycopg2-binary

# 2026-09-10: voz clonada do Meishu-Sama (Fish Audio) — provedor PREFERIDO da
# voz "meishu". Faltava aqui: estava instalado só no .venv (usado pela geração
# em lote), e o serviço roda no venv/ → `from fishaudio import FishAudio`
# falhava com ModuleNotFoundError e o sintetizador caía no fallback (voz
# errada, lenta: ~70 s por trecho em vez de ~3 s). Ver HANDOFF.
fish-audio-sdk==1.3.0
